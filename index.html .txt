<!DOCTYPE html>
<html>
<head>
    <title>NOVA</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            max-width: 700px;
            margin: 0 auto;
            padding: 20px;
            background: #0f0f1a;
            color: #fff;
        }
        h1 {
            text-align: center;
            color: #00d4ff;
            letter-spacing: 2px;
        }
        #chat-box {
            background: #1a1a2e;
            height: 420px;
            overflow-y: auto;
            padding: 15px;
            border-radius: 12px;
            margin-bottom: 15px;
            border: 1px solid #00d4ff33;
        }
        .user-msg {
            text-align: right;
            margin: 10px 0;
        }
        .user-msg span {
            background: #00d4ff;
            color: #0f0f1a;
            padding: 8px 14px;
            border-radius: 14px;
            display: inline-block;
            max-width: 80%;
        }
        .bot-msg {
            text-align: left;
            margin: 10px 0;
        }
        .bot-msg span {
            background: #2a2a40;
            color: #fff;
            padding: 8px 14px;
            border-radius: 14px;
            display: inline-block;
            max-width: 80%;
        }
        #input-area {
            display: flex;
            gap: 10px;
        }
        #user-input {
            flex: 1;
            padding: 12px;
            border-radius: 10px;
            border: 1px solid #00d4ff55;
            background: #1a1a2e;
            color: #fff;
            font-size: 14px;
        }
        button {
            padding: 12px 22px;
            background: #00d4ff;
            color: #0f0f1a;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 14px;
            font-weight: bold;
        }
        .play-btn {
            background: #444;
            padding: 4px 10px;
            font-size: 11px;
            border-radius: 6px;
            cursor: pointer;
            border: none;
            color: white;
            margin-left: 8px;
        }
        #typing {
            color: #888;
            font-size: 12px;
            padding-left: 10px;
            display: none;
        }
    </style>
</head>
<body>
    <h1>NOVA 🤖</h1>
    <div id="chat-box"></div>
    <div id="typing">NOVA is thinking...</div>
    <div id="input-area">
        <input type="text" id="user-input" placeholder="Talk to NOVA..."/>
        <button onclick="sendMessage()">Send</button>
    </div>
    <script>
        async function sendMessage() {
            const input = document.getElementById("user-input");
            const chatBox = document.getElementById("chat-box");
            const typing = document.getElementById("typing");
            const message = input.value.trim();
            if (!message) return;
            chatBox.innerHTML += `<div class="user-msg"><span>${message}</span></div>`;
            input.value = "";
            chatBox.scrollTop = chatBox.scrollHeight;
            typing.style.display = "block";

            const response = await fetch("/chat", {
                method: "POST",
                headers: {"Content-Type": "application/json"},
                body: JSON.stringify({message: message})
            });
            const data = await response.json();
            typing.style.display = "none";
            chatBox.innerHTML += `<div class="bot-msg"><span>${data.reply}</span> <button class="play-btn" onclick="playAudio()">🔊</button></div>`;
            chatBox.scrollTop = chatBox.scrollHeight;
        }
        function playAudio() {
            const audio = new Audio("/static/reply.mp3?t=" + new Date().getTime());
            audio.play();
        }
        document.getElementById("user-input").addEventListener("keypress", function(e) {
            if (e.key === "Enter") sendMessage();
        });
    </script>
</body>
</html>